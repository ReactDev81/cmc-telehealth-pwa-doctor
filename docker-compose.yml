services:
  nextjs-app:
    build:
      context: .
      # args:
      #   NEXT_PUBLIC_API_BASE_URL: https://telehealthwebapplive.cmcludhiana.in/api/v2

    container_name: nextjs-app

    ports:
      - "8000:3000"

    restart: always
